Commands run (from repo root):

# build functions and client (this is the project's build step)
npm run build

# direct functions build used during earlier investigation
npm run build:functions

# attempt deploys used during debugging
npx netlify deploy --prod --dir=dist --skip-functions-cache --debug
npx netlify deploy --prod --dir=dist --skip-functions-cache --no-build --debug

# netlify CLI status check
netlify status --verbose
